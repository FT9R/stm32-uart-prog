{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "delete-logs",
            "type": "shell",
            "command": "rm -rf ${workspaceFolder}/logs",
            "windows": {
                "command": "if (Test-Path ${workspaceFolder}\\logs) {Remove-Item -Recurse -Force ${workspaceFolder}\\logs} else {echo 'Logs directory does not exist'}"
            },
            "problemMatcher": []
        }
    ]
}